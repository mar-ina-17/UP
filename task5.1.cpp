#include "iostream"
 using namespace std;
 int main(){
     double a,b,c;
     cin>> a>> b >> c;
     if(a>b&& a>c) cout<<"a is the nai-golqmo";
     if(b>a && b>c) cout<<"b is ";
     else if (c>a && c>b)cout<<"c is";
     return 0;
 }